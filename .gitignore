# Rahasia — JANGAN commit (berisi OANDA_TOKEN)
.env
*.local

# Catatan deploy lokal (berisi IP/instance ID + info keamanan)
DEPLOY-NOTES.md

# Roadmap kerja lokal (memuat catatan keamanan/infra)
ROADMAP.md

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Build Go
/bar-replay
*.exe

# SQLite (persistensi live trading — data lokal, jangan commit)
*.db
*.db-wal
*.db-shm
