# Salin ke /etc/bar-replay.env di instance, isi nilainya, lalu chmod 600.
# Dibaca systemd lewat EnvironmentFile. JANGAN commit yang berisi token asli.
OANDA_TOKEN=
OANDA_ENV=practice
PORT=8765
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
